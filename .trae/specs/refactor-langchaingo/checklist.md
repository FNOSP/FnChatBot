\*- \[x] 已安装 LangChainGo 依赖项 (`go.mod` 已更新)

* [x] 已实现 `SQLiteChatMessageHistory` 并通过 `schema.ChatMessageHistory` 接口验证

* [x] 已创建 `LangChainService`（或等效服务），并能实例化 OpenAI/Anthropic 模型

* [x] 聊天 API 端点成功使用 `LangChainGo` 生成回复

* [x] 聊天记录正确保存到 SQLite 的 `messages` 表中

* [x] 流式响应通过 `LangChainGo` 回调正常工作

