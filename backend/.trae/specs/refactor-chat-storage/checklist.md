- [x] `Session` 表创建成功，且包含必要字段（如 UUID/ID, Title）。
- [x] `Message` 表创建成功，关联到 `Session`。
- [x] `Part` 表创建成功，关联到 `Message`，且支持存储 JSON 内容。
- [x] 发送纯文本消息时，数据库中能正确生成 1 个 `Message` 和 1 个 `text` 类型的 `Part`。
- [x] 发送包含工具调用的消息时，数据库中能正确生成 `tool_call` 类型的 `Part`。
- [x] 获取历史记录接口能正确返回完整的对话上下文（包括工具调用）。
